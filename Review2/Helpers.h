#pragma once
#include<cstring>
#include<iostream>
#include<string>
using namespace std;

void Append(char*, char*);
void Insert(char*, char*, int);
int WordCount(char*);
void Break(char*, int);
char ToLower(char);